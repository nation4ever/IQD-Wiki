---
title: الواجهة الخلفية
description: كل ما يعمل في الخادم - APIs، قواعد البيانات، المصادقة
---

# الواجهة الخلفية (Back-end) ⚙️

الواجهة الخلفية هي "المحرك" الخفي للتطبيق. كل الأشياء التي لا يراها المستخدم لكنها ضرورية لعمل التطبيق.

## 🎯 ما الذي يفعله مطور الخلفية؟

- **إدارة قواعد البيانات**: تخزين واسترجاع البيانات
- **بناء APIs**: نقاط الاتصال بين الواجهة الأمامية والخادم
- **المصادقة والأمان**: حماية التطبيق والمستخدمين
- **معالجة الملفات**: رفع وتخزين الصور والوثائق
- **إرسال الإشعارات**: البريد الإلكتروني، الرسائل القصيرة

## 🛠️ اللغات والأُطر الشائعة

### Node.js (JavaScript)

نفس اللغة للواجهتين، مجتمع ضخم.

```javascript
import express from "express";

const app = express();

app.get("/api/products", async (req, res) => {
  const products = await db.products.findMany();
  res.json(products);
});

app.post("/api/products", async (req, res) => {
  const newProduct = await db.products.create({
    data: req.body,
  });
  res.status(201).json(newProduct);
});

app.listen(3000, () => {
  console.log("الخادم يعمل على المنفذ 3000 🚀");
});
```

### Python (FastAPI / Django)

سهل القراءة، قوي للبيانات والذكاء الاصطناعي.

```python
from fastapi import FastAPI
from pydantic import BaseModel

app = FastAPI()

class Product(BaseModel):
    name: str
    price: float
    description: str | None = None

@app.get("/api/products")
async def get_products():
    return await db.products.find_many()

@app.post("/api/products")
async def create_product(product: Product):
    created = await db.products.create(data=product.dict())
    return created
```

## 🗃️ قواعد البيانات

### SQL (علائقية)

البيانات في جداول مترابطة. الأفضل للبيانات المنظمة.

```sql
-- إنشاء جدول المنتجات
CREATE TABLE products (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL,
  price DECIMAL(10, 2) NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);

-- إضافة منتج جديد
INSERT INTO products (name, price)
VALUES ('قميص قطني', 99.00);

-- جلب جميع المنتجات
SELECT * FROM products WHERE price < 100;
```

**أشهر الخيارات**: PostgreSQL، MySQL، SQLite

### NoSQL (غير علائقية)

بيانات مرنة بدون هيكل ثابت. الأفضل للتوسع الأفقي.

```javascript
// MongoDB
const product = {
  name: "قميص قطني",
  price: 99,
  variants: [
    { size: "S", color: "أبيض" },
    { size: "M", color: "أسود" },
  ],
};

await db.products.insertOne(product);
```

**أشهر الخيارات**: MongoDB، Redis، Firebase

## 🔐 المصادقة والأمان

### JWT (JSON Web Tokens)

```javascript
import jwt from "jsonwebtoken";

function generateToken(user) {
  return jwt.sign({ id: user.id, email: user.email }, process.env.JWT_SECRET, {
    expiresIn: "7d",
  });
}

function verifyToken(token) {
  return jwt.verify(token, process.env.JWT_SECRET);
}
```

### نصائح أمان مهمة

1. **لا تخزن كلمات المرور كنص عادي**: استخدم bcrypt
2. **التحقق من المدخلات**: لا تثق بأي بيانات من المستخدم
3. **استخدم HTTPS دائماً**: شفر الاتصالات
4. **حدد صلاحيات**: لا تعطِ المستخدم أكثر مما يحتاج

## 📡 أنماط APIs

### REST API

الأكثر شيوعاً، يستخدم HTTP methods.

```
GET    /api/products      → جلب جميع المنتجات
GET    /api/products/1    → جلب منتج محدد
POST   /api/products      → إنشاء منتج جديد
PUT    /api/products/1    → تحديث منتج
DELETE /api/products/1    → حذف منتج
```

### GraphQL

طلب البيانات التي تحتاجها فقط.

```graphql
query {
  product(id: 1) {
    name
    price
  }
}
```

## 🚀 الخطوات التالية

1. اختر لغة (Node.js إذا كنت تعرف JavaScript)
2. تعلم أساسيات HTTP وكيف تعمل الـ APIs
3. ابنِ API بسيط (CRUD لمنتجات أو مهام)
4. أضف قاعدة بيانات
5. أضف المصادقة
