---
title: أدوات المطورين
description: الأدوات الأساسية التي يحتاجها كل مبرمج
---

# أدوات المطورين 🛠️

الأدوات الصحيحة تجعل عملك أسهل وأسرع وأكثر متعة! هنا ستجد دليلاً شاملاً لأهم الأدوات التي يستخدمها المطورون.

## 💻 محررات الأكواد

### Visual Studio Code (الأفضل)

محرر مجاني من مايكروسوفت، الأكثر شعبية بين المطورين.

**المميزات:**

- خفيف وسريع
- آلاف الإضافات
- دعم ممتاز لكل اللغات
- تكامل مع Git
- طرفية مدمجة

**الإضافات الموصى بها:**

- **Prettier** - تنسيق الكود تلقائياً
- **ESLint** - اكتشاف الأخطاء
- **GitLens** - معلومات Git متقدمة
- **Auto Rename Tag** - تعديل وسوم HTML معاً
- **Arabic Formatter** - دعم اللغة العربية

### بدائل أخرى

- **WebStorm** - للمحترفين (مدفوع)
- **Sublime Text** - خفيف وسريع جداً
- **Vim/Neovim** - للخبراء

## 🖥️ الطرفية (Terminal)

الطرفية هي واجهة سطر الأوامر، أداة قوية جداً للمطورين.

### أوامر أساسية

```bash
# التنقل
cd folder         # الدخول لمجلد
cd ..             # الرجوع للمجلد السابق
pwd               # عرض المسار الحالي

# الملفات
ls                # عرض الملفات
mkdir folder      # إنشاء مجلد
touch file.txt    # إنشاء ملف
rm file.txt       # حذف ملف
rm -rf folder     # حذف مجلد

# مساعدة
man command       # دليل الأمر
command --help    # مساعدة سريعة
```

## 🔄 Git - نظام التحكم بالإصدارات

Git يحفظ تاريخ مشروعك ويسمح بالتعاون مع الآخرين.

### الأوامر الأساسية

```bash
# البداية
git init              # بدء مشروع جديد
git clone <url>       # نسخ مشروع موجود

# التعديلات
git status            # حالة الملفات
git add .             # إضافة كل التعديلات
git commit -m "رسالة" # حفظ التعديلات

# الفروع
git branch            # عرض الفروع
git checkout -b new   # إنشاء فرع جديد
git merge branch      # دمج فرع

# المزامنة
git pull              # جلب التحديثات
git push              # رفع التحديثات
```

### سير العمل المثالي

```
1. git pull             # جلب آخر التحديثات
2. git checkout -b feature/new-feature
3. [العمل على الكود]
4. git add .
5. git commit -m "feat: add new feature"
6. git push origin feature/new-feature
7. [فتح Pull Request]
```

## 📦 مديرو الحزم

### npm (Node.js)

```bash
npm init -y           # بدء مشروع
npm install package   # تثبيت حزمة
npm install -D pkg    # حزمة للتطوير فقط
npm run script        # تشغيل سكربت
```

### pnpm (أسرع من npm)

```bash
pnpm install          # تثبيت الحزم
pnpm add package      # إضافة حزمة
pnpm run dev          # تشغيل سكربت
```

### bun (الأحدث والأسرع)

```bash
bun init              # بدء مشروع
bun add package       # إضافة حزمة
bun run dev           # تشغيل سكربت
bun install           # تثبيت الحزم
```

## 🌐 أدوات المتصفح

### Chrome DevTools

اضغط `F12` لفتح أدوات المطورين:

- **Elements**: فحص وتعديل HTML/CSS
- **Console**: تنفيذ JavaScript
- **Network**: مراقبة الطلبات
- **Performance**: تحليل الأداء
- **Application**: Storage و Cookies

### إضافات مفيدة

- **React Developer Tools** - لتطبيقات React
- **Vue.js devtools** - لتطبيقات Vue
- **JSON Formatter** - عرض JSON بشكل جميل
- **Wappalyzer** - معرفة تقنيات الموقع

## 🎨 أدوات التصميم

- **Figma** - تصميم الواجهات (مجاني)
- **Excalidraw** - رسم المخططات
- **ColorHunt** - ألوان جاهزة
- **Coolors** - توليد ألوان متناسقة

## 📱 أدوات مفيدة أخرى

- **Postman/Insomnia** - اختبار APIs
- **Docker** - تشغيل البيئات المعزولة
- **Notion/Obsidian** - تدوين الملاحظات
- **ChatGPT/Claude** - مساعدة في الكود

## ✅ الإعداد المثالي للمبتدئ

1. **حمّل VS Code**
2. **ثبّت Node.js و bun**
3. **تعلم الأوامر الأساسية للطرفية**
4. **تعلم Git الأساسي**
5. **استكشف DevTools في المتصفح**
