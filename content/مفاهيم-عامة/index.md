---
title: مفاهيم عامة
description: أساسيات علوم الحاسب والبرمجة التي يجب أن يعرفها كل مطور
---

# مفاهيم عامة في علوم الحاسب 🧠

فهم المفاهيم الأساسية يجعلك مبرمجاً أفضل. هنا تجد أهم المفاهيم التي تحتاجها.

## 🔢 كيف يعمل الحاسوب

### الثنائي (Binary)

الحاسوب يفهم فقط 0 و 1 (كهرباء مفتوحة/مغلقة).

```
الرقم 5 = 101 بالثنائي
الحرف A = 01000001 بالثنائي
```

### وحدات القياس

| الوحدة | الحجم       | مثال         |
| ------ | ----------- | ------------ |
| Bit    | 0 أو 1      | أصغر وحدة    |
| Byte   | 8 bits      | حرف واحد     |
| KB     | 1,024 bytes | صفحة نصية    |
| MB     | 1,024 KB    | أغنية        |
| GB     | 1,024 MB    | فيلم         |
| TB     | 1,024 GB    | آلاف الأفلام |

## 🏗️ هياكل البيانات

### المصفوفة (Array)

قائمة مرتبة من العناصر.

```javascript
const fruits = ["تفاح", "برتقال", "موز"];
fruits[0]; // تفاح
fruits.push("عنب"); // إضافة عنصر
```

**متى تستخدمها؟** عندما تحتاج قائمة مرتبة.

### الكائن (Object)

بيانات منظمة بمفاتيح وقيم.

```javascript
const user = {
  name: "أحمد",
  age: 25,
  email: "ahmed@example.com",
};
user.name; // أحمد
```

**متى تستخدمه؟** عندما تحتاج بيانات مترابطة.

### المكدس (Stack)

آخر من يدخل، أول من يخرج (LIFO).

```
│ 3 │ ← الأخير (سيخرج أولاً)
│ 2 │
│ 1 │ ← الأول
└───┘
```

**مثال:** زر الرجوع في المتصفح.

### الطابور (Queue)

أول من يدخل، أول من يخرج (FIFO).

```
← [1] [2] [3] ←
   ↓         ↑
  خروج     دخول
```

**مثال:** طابور الطلبات في مطعم.

## ⏱️ تعقيد الخوارزميات (Big O)

يقيس كفاءة الخوارزمية مع زيادة البيانات.

| التعقيد  | الاسم     | مثال                  |
| -------- | --------- | --------------------- |
| O(1)     | ثابت      | الوصول لعنصر في array |
| O(log n) | لوغاريتمي | البحث الثنائي         |
| O(n)     | خطي       | البحث في قائمة        |
| O(n²)    | تربيعي    | حلقتين متداخلتين      |

```javascript
// O(1) - ثابت
const first = array[0];

// O(n) - خطي
for (const item of array) {
  console.log(item);
}

// O(n²) - تربيعي
for (let i = 0; i < n; i++) {
  for (let j = 0; j < n; j++) {
    console.log(i, j);
  }
}
```

## 🌐 كيف يعمل الإنترنت

### نموذج العميل والخادم

```
┌──────────┐         طلب          ┌──────────┐
│  العميل  │ ────────────────────► │  الخادم  │
│ (المتصفح)│ ◄──────────────────── │ (Server) │
└──────────┘        استجابة        └──────────┘
```

### بروتوكول HTTP

| الفعل  | الاستخدام    | مثال       |
| ------ | ------------ | ---------- |
| GET    | جلب بيانات   | عرض صفحة   |
| POST   | إرسال بيانات | تسجيل دخول |
| PUT    | تحديث كامل   | تعديل ملف  |
| PATCH  | تحديث جزئي   | تغيير اسم  |
| DELETE | حذف          | حذف مستخدم |

### رموز الحالة (Status Codes)

- **2xx** - نجاح (200 OK, 201 Created)
- **3xx** - تحويل (301 Moved, 304 Not Modified)
- **4xx** - خطأ من العميل (404 Not Found, 401 Unauthorized)
- **5xx** - خطأ من الخادم (500 Internal Error)

## 🔐 أساسيات الأمان

### التشفير (Encryption)

تحويل البيانات لصيغة غير مقروءة.

```
"مرحبا" ──► [تشفير] ──► "x7#kL9@m"
```

### الهاش (Hashing)

تحويل في اتجاه واحد (لا يمكن عكسه).

```
"password123" ──► [هاش] ──► "a1b2c3d4e5..."
```

**الاستخدام:** تخزين كلمات المرور.

### HTTPS

HTTP + تشفير = اتصال آمن.

> ⚠️ لا تدخل بياناتك في مواقع HTTP فقط!

## 📝 مبادئ البرمجة الجيدة

### DRY - لا تكرر نفسك

```javascript
// ❌ سيء
console.log("مرحباً أحمد");
console.log("مرحباً سارة");

// ✅ جيد
const greet = (name) => console.log(`مرحباً ${name}`);
greet("أحمد");
greet("سارة");
```

### KISS - اجعله بسيطاً

الحل البسيط غالباً هو الأفضل.

### التسمية الواضحة

```javascript
// ❌ سيء
const x = users.filter((u) => u.a > 18);

// ✅ جيد
const adults = users.filter((user) => user.age > 18);
```

## 🎯 الخطوات التالية

- تعلم هياكل بيانات أكثر (Trees, Graphs)
- تدرب على حل مسائل في LeetCode
- افهم كيف يعمل نظام التشغيل
- تعرف على الشبكات بشكل أعمق
